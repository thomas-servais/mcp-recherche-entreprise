# Configuration pour le serveur MCP data.gouv.fr
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# URL de base de l'API data.gouv.fr (ne pas modifier sauf si l'API change)
DATA_GOUV_API_BASE_URL=https://www.data.gouv.fr/api/1

# Limite de taux pour les requêtes API (optionnel)
API_RATE_LIMIT=100

# Timeout pour les requêtes API en millisecondes
API_TIMEOUT=10000
